{
  "name": "Copilot Coding Agent Activity Dashboard",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {},
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "forwardPorts": [8080],
  "portsAttributes": {
    "8080": {
      "label": "Vite Dev Server",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": ". ${NVM_DIR}/nvm.sh && NODE_VERSION=$(cat .node-version) && nvm install $NODE_VERSION && nvm use $NODE_VERSION && npm install && npx playwright install --with-deps chromium",
  "customizations": {
    "vscode": {
      "extensions": [
        "bradlc.vscode-tailwindcss",
        "ms-playwright.playwright",
        "GitHub.copilot",
        "GitHub.copilot-chat"
      ],
      "settings": {
        "tailwindCSS.includeLanguages": {
          "html": "html",
          "javascript": "javascript"
        }
      }
    }
  }
}
